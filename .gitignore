# Dependencies (Thư viện tải về - Rất nặng, không commit)
/node_modules
/.pnp
.pnp.js

# Testing
/coverage

# Next.js (File build ra - Không commit)
/.next/
/out/

# Production
/build

# Misc (File rác hệ điều hành)
.DS_Store
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment Variables (QUAN TRỌNG: Chứa key bảo mật, TUYỆT ĐỐI KHÔNG commit)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Vercel
.vercel